version: '3.8'

services:
  # PostgreSQL数据库
  postgres:
    image: postgres:15-alpine
    container_name: onboardkit-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: onboardkit
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - onboardkit-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis缓存
  redis:
    image: redis:7-alpine
    container_name: onboardkit-redis
    ports:
      - "6379:6379"
    networks:
      - onboardkit-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # 后端服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: onboardkit-backend
    environment:
      POSTGRES_SERVER: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: onboardkit
      REDIS_HOST: redis
      REDIS_PORT: 6379
      SECRET_KEY: ${SECRET_KEY:-your-secret-key-change-in-production}
      BACKEND_CORS_ORIGINS: '["http://106.75.222.205", "http://localhost:5173", "http://localhost"]'
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - onboardkit-network
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  # 前端服务
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: onboardkit-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - onboardkit-network

volumes:
  postgres_data:

networks:
  onboardkit-network:
    driver: bridge

# 数据库健康检查和自动重启配置
# 如果数据库出现问题，可以运行以下命令重建：
# docker compose stop postgres
# docker compose rm -f postgres
# docker volume rm onboard-kit_postgres_data
# docker compose up -d postgres
# docker exec onboardkit-backend python3 -c "
# import asyncio
# from app.core.database import engine
# from app.models.models import Base
# async def init():
#     async with engine.begin() as conn:
#         await conn.run_sync(Base.metadata.create_all)
#     print(Database initialized)
# asyncio.run(init())
# "
